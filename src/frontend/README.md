# Frontend del Sistema Clínico Dental

La instalación, pruebas y arquitectura están en el [README del proyecto](../../README.md).

Producción: construir con `VITE_API_URL=/` y publicar frontend y `/api/` bajo un solo origen HTTPS. Configurar el proxy siguiendo [deployment.md](../../docs/deployment.md). No hay un destino de demo activo en esta carpeta; su plantilla está en `deployment/vercel.demo.json.template`.
